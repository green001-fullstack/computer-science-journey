# When BFS is a better choice

### WHen you are trying the find the closest distance between two nodes

# When DFS is a better choice

### When you want to explore different paths.

# Time complexity comparison

### O(n) for both

# Space complexity comparison

### O(h) for DFS and O(w) for BFS

# The biggest difference between the two

### DFS explores one path as deeply as possible before backtracking, while BFS explores every node at the current level before moving to the next level.

# My Biggest Insight

### I figured that out that both DFS and BFS are good algorithms for traversals but their usage depends on what problem you want to solve. 